import 'package:flutter/material.dart';

class ChessNews extends StatelessWidget {
  final Color color;

  ChessNews(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
