import 'package:flutter/material.dart';

class LearnChess extends StatelessWidget {
  final Color color;

  LearnChess(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
